package main //o go entende que o pacote executavel obrigatoriamente se chama main

import (
	"fmt"
	/* "strings" */
) //importação de maneira muito simples, pois não usa virgulas e sempre dentro de parenteses

func main() { //função para poder chamar no terminal e executar o arquivo
	fmt.Println("Hello, World!")
}

